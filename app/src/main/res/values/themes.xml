<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AgendamentoApp" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Substitui a splash automática do Android 12+ (ícone do launcher) por fundo só + ícone invisível. -->
    <style name="Theme.AgendamentoApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_empty</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.AgendamentoApp</item>
    </style>
</resources>